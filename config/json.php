<?php

declare(strict_types=1);

/*
 * Reserved for future package-level defaults.
 *
 * JsonColumn is currently configured through its fluent API, but the config
 * file remains publishable for backward compatibility with the package
 * installer and existing host applications.
 */
return [
];
